# ============================================================
# ESP32 Voice AI - PC 端依赖
# ============================================================
#
# 音频处理
numpy>=1.21.0
soundfile>=0.12.0
scipy>=1.7.0
pyserial>=3.5
#
# TTS 语音合成
edge-tts>=6.0.0
pydub>=0.25.0
#
# 麦克风录音
sounddevice>=0.4.6
#
# LLM 对话
requests>=2.28.0
#
# 配置管理
python-dotenv>=1.0.0
#
# ============================================================
# 系统依赖 (非 pip):
#   ffmpeg   - pydub MP3→WAV 转换 (apt install ffmpeg)
#   ALSA     - sounddevice 音频输入 (apt install libasound2-dev)
#   portaudio- sounddevice 底层音频库 (apt install portaudio19-dev)
# ============================================================
# 安装方法:
#   python3 -m venv .venv
#   source .venv/bin/activate       # Linux/macOS
#   .venv\Scripts\activate.bat      # Windows
#   pip install -r requirements.txt
# ============================================================
